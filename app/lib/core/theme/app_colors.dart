import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- Background ---
  static const background = Color(0xFF0F0F1C);
  static const surface = Color(0xFF16162A);
  static const surfaceVariant = Color(0xFF1A1A2E);

  // --- Primary (Purple gradient base) ---
  static const primary = Color(0xFF7C3AED);
  static const primaryLight = Color(0xFFA78BFA);
  static const primaryDark = Color(0xFF4F46E5);

  // --- Accent ---
  static const accent = Color(0xFF60A5FA);

  // --- Category colors ---
  // Rock (purple tone - same as primary)
  static const rock = Color(0xFFA78BFA);
  static const rockBg = Color(0x1AA78BFA);
  static const rockBorder = Color(0x4DA78BFA);

  // Pebble (amber/yellow)
  static const pebble = Color(0xFFFCD34D);
  static const pebbleBg = Color(0x1AFCD34D);
  static const pebbleBorder = Color(0x40FCD34D);

  // Sand (green)
  static const sand = Color(0xFF34D399);
  static const sandBg = Color(0x1A34D399);
  static const sandBorder = Color(0x4034D399);

  // --- Status ---
  static const danger = Color(0xFFF87171);
  static const dangerBg = Color(0x1AF87171);
  static const dangerBorder = Color(0x33F87171);

  static const success = Color(0xFF34D399);
  static const warning = Color(0xFFFCD34D);

  // --- Text ---
  static const textPrimary = Color(0xFFF1F5F9);
  static const textSecondary = Color(0xFF94A3B8);
  static const textDisabled = Color(0xFF475569);
  static const textMuted = Color(0xFF334155);

  // --- Border / Divider ---
  static const border = Color(0x0FFFFFFF);
  static const divider = Color(0x0FFFFFFF);
}
